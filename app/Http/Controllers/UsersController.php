<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class UsersController extends Controller
{
     //  $token = $request->user()->createToken($request->token_name , ['articles:create'] )->plainTextToken;

    //  ->createToken($request->token_name , ['articles:create'] )->plainTextToken
}
